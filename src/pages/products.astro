---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import GradientBackground from '../components/GradientBackground.astro';
import ThemeToggle from '../components/ThemeToggle.astro';

// 产品数据
const products = [
  {
    id: 'jackproxy',
    title: 'JackProxy',
    description: '专业轮转代理池管理工具，50万+代理IP服务，为渗透测试、攻防对抗、SRC挖掘提供稳定高效的代理解决方案',
    badges: [{ text: 'v2.0', color: 'green' }, { text: '稳定版', color: 'purple' }],
    features: [
      { text: '智能代理轮换', color: 'blue' },
      { text: '实时状态监控', color: 'green' },
      { text: '一键操作体验', color: 'purple' },
      { text: '美观沉浸式UI', color: 'amber' }
    ],
    featureTitle: '核心特性',
    gradient: { from: 'blue', to: 'purple' },
    icon: { type: 'image', src: '/jackproxy-icon.png' },
    actions: [{ text: '🎬 演示视频', href: 'https://www.bilibili.com/video/BV1gJ7DzWE9f', external: true }]
  },
  {
    id: 'antivirus',
    title: '专业免杀服务',
    description: '一线大厂攻防实战经验总结，专业的反杀软对抗服务。一马通杀国内所有主流杀软，采用白加黑技术，经过大量实战验证，确保木马对各类杀软均可有效免杀。',
    badges: [{ text: '定制服务', color: 'red' }, { text: '实战验证', color: 'green' }],
    features: [
      { text: '360安全卫士', color: 'green' },
      { text: '腾讯电脑管家', color: 'blue' },
      { text: '火绒安全', color: 'orange' },
      { text: 'Windows Defender', color: 'cyan' },
      { text: '金山毒霸', color: 'yellow' },
      { text: '天融信EDR/亿格云', color: 'purple' }
    ],
    featureTitle: '支持杀软',
    features2: [
      { text: '全自动权限维持', color: 'cyan' },
      { text: '反微步沙箱检测', color: 'pink' },
      { text: '白加黑技术方案', color: 'indigo' },
      { text: '16种钓鱼教学资料', color: 'teal' }
    ],
    gradient: { from: 'red', to: 'pink' },
    icon: { type: 'video', src: '/antivirus-icon.mp4' },
    actions: [{ text: '🎬 效果演示', href: 'https://www.bilibili.com/video/BV1dM17Y8EXc', external: true }]
  },
  {
    id: 'snowlight',
    title: 'SnowLight',
    description: '原生免杀360的Rust C2框架，纯自研项目，持续开发中...',
    badges: [{ text: '开发中', color: 'amber', pulse: true }],
    features: [
      { text: 'Rust原生开发', color: 'cyan' },
      { text: '原生免杀360', color: 'blue' }
    ],
    featureTitle: '计划特性',
    gradient: { from: 'cyan', to: 'blue' },
    icon: { type: 'video', src: '/snowlight-icon.mp4' },
    actions: [{ text: '🎬 开发预览', href: 'https://www.bilibili.com/video/BV1K5xNzzE1x', external: true }]
  },
  {
    id: 'aisql',
    title: 'Yingxue AiSQL',
    description: 'AI 驱动的 SQL 注入自动化检测与利用工具，集成传统漏洞检测技术与 AI 智能利用能力',
    badges: [{ text: 'v1.0', color: 'green' }, { text: '稳定版', color: 'purple' }],
    features: [
      { text: 'AI 智能利用', color: 'orange' },
      { text: '全流程自动化', color: 'red' },
      { text: '多种注入类型', color: 'amber' },
      { text: '现代化 Web 界面', color: 'purple' }
    ],
    featureTitle: '核心特性',
    gradient: { from: 'orange', to: 'red' },
    icon: { type: 'video', src: '/aisql-icon.mp4' },
    actions: [{ text: '📦 立即下载', href: 'https://c.wss.cc/f/ii5ewezrffo', external: true }]
  }
];
---

<Layout title="产品 - 映雪安全" description="映雪安全团队开发的安全工具和产品">
  <GradientBackground color1="blue" color2="purple" />
  
  <Navbar currentPage="products">
    <ThemeToggle slot="theme-toggle" />
  </Navbar>

  <!-- 页面标题 -->
  <section class="container mx-auto relative px-6 py-20">
    <div class="mx-auto flex max-w-[980px] flex-col items-center gap-6 text-center">
      <div class="fade-in-up inline-flex items-center rounded-full bg-gradient-to-r from-primary/20 to-purple-500/20 px-4 py-2 text-sm font-medium border border-primary/20 backdrop-blur-sm">
        <span class="mr-2 text-lg">🛠️</span>
        <span class="text-primary">专业安全工具</span>
      </div>
      
      <h1 class="fade-in-up delay-100 text-4xl font-bold leading-tight tracking-tighter md:text-5xl lg:text-6xl">
        我们的<span class="gradient-text-animated">产品</span>
      </h1>
      
      <p class="fade-in-up delay-200 max-w-[750px] text-lg text-muted-foreground leading-relaxed">
        专注于开发实用、高效的网络安全工具，帮助安全研究人员和团队提升工作效率
      </p>
    </div>
  </section>

  <!-- 产品列表 -->
  <section class="container mx-auto px-6 pb-24">
    <div class="mx-auto max-w-6xl space-y-8">
      {products.map((product) => (
        <div class="group relative overflow-hidden rounded-2xl border border-border/50 bg-card/50 backdrop-blur-xl transition-all hover-card">
          <div class={`absolute inset-0 bg-gradient-to-br from-${product.gradient.from}-500/5 via-transparent to-${product.gradient.to}-500/5 opacity-0 group-hover:opacity-100 transition-opacity duration-700`}></div>
          <div class={`absolute top-0 right-0 w-64 h-64 bg-${product.gradient.from}-500/5 rounded-full blur-[80px] -translate-y-1/2 translate-x-1/2`}></div>
          
          <div class="relative grid gap-8 p-8 md:p-10 md:grid-cols-[180px_1fr]">
            <div class="flex items-center justify-center">
              {product.icon.type === 'image' ? (
                <img src={product.icon.src} alt={product.title} class="h-40 w-40 rounded-2xl shadow-xl transition-transform group-hover:scale-105" />
              ) : product.icon.type === 'video' ? (
                <video src={product.icon.src} autoplay loop muted playsinline class="h-40 w-40 rounded-2xl shadow-xl transition-transform group-hover:scale-105 object-cover" />
              ) : (
                <div class={`h-40 w-40 rounded-2xl bg-gradient-to-br from-${product.gradient.from}-500 to-${product.gradient.to}-500 shadow-xl flex items-center justify-center transition-transform group-hover:scale-105`}>
                  <Fragment set:html={product.icon.svg} />
                </div>
              )}
            </div>
            
            <div class="space-y-5">
              <div>
                <h3 class="text-2xl font-bold group-hover:text-primary transition-colors">{product.title}</h3>
                <div class="mt-3 flex items-center gap-2">
                  {product.badges.map((badge) => (
                    <span class={`inline-flex items-center rounded-full bg-${badge.color}-500/10 px-3 py-1 text-xs font-medium text-${badge.color}-400 ring-1 ring-inset ring-${badge.color}-500/20 ${badge.pulse ? 'animate-pulse' : ''}`}>
                      {badge.text}
                    </span>
                  ))}
                </div>
              </div>
              
              <p class="text-muted-foreground leading-relaxed">{product.description}</p>
              
              <div class="space-y-3">
                <h4 class="font-semibold text-foreground/90">{product.featureTitle}</h4>
                <ul class="grid gap-2 text-sm sm:grid-cols-2">
                  {product.features.map((feature) => (
                    <li class="flex items-center gap-3 p-2.5 rounded-lg bg-card/50 border border-border/30">
                      <div class={`flex h-6 w-6 items-center justify-center rounded bg-${feature.color}-500/10`}>
                        <svg class={`h-3.5 w-3.5 text-${feature.color}-400`} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                        </svg>
                      </div>
                      <span class="text-muted-foreground">{feature.text}</span>
                    </li>
                  ))}
                </ul>
              </div>

              {product.features2 && (
                <div class="space-y-3">
                  <h4 class="font-semibold text-foreground/90">服务特性</h4>
                  <ul class="grid gap-2 text-sm sm:grid-cols-2">
                    {product.features2.map((feature) => (
                      <li class="flex items-center gap-3 p-2.5 rounded-lg bg-card/50 border border-border/30">
                        <div class={`flex h-6 w-6 items-center justify-center rounded bg-${feature.color}-500/10`}>
                          <svg class={`h-3.5 w-3.5 text-${feature.color}-400`} fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                          </svg>
                        </div>
                        <span class="text-muted-foreground">{feature.text}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              )}
              
              <div class="flex items-center gap-4 pt-4">
                <a href={`/products/${product.id}`} class="inline-flex h-9 items-center justify-center rounded-lg bg-primary px-4 text-sm font-medium text-primary-foreground transition-all hover:bg-primary/90 hover:scale-105">
                  查看详情
                </a>
                {product.actions.map((action) => (
                  <a href={action.href} target={action.external ? "_blank" : undefined} class="inline-flex h-9 items-center justify-center rounded-lg border border-border bg-background px-4 text-sm font-medium transition-colors hover:bg-accent">
                    {action.text}
                  </a>
                ))}
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- 引导区域 -->
  <section class="container mx-auto px-6 pb-24">
    <div class="mx-auto max-w-4xl">
      <div class="group relative overflow-hidden rounded-2xl border border-border/50 bg-gradient-to-r from-primary/10 via-purple-500/10 to-pink-500/10 p-8 md:p-10 backdrop-blur-xl">
        <!-- 装饰性背景 -->
        <div class="absolute top-0 right-0 w-64 h-64 bg-primary/10 rounded-full blur-[80px] -translate-y-1/2 translate-x-1/2"></div>
        <div class="absolute bottom-0 left-0 w-48 h-48 bg-purple-500/10 rounded-full blur-[60px] translate-y-1/2 -translate-x-1/2"></div>
        
        <div class="relative flex flex-col md:flex-row items-center justify-between gap-8">
          <div class="flex-1 space-y-4">
            <div class="flex items-center gap-3">
              <span class="text-4xl">🎁</span>
              <h3 class="text-2xl font-bold">加入技术交流群，解锁更多福利</h3>
            </div>
            <p class="text-muted-foreground leading-relaxed">
              与安全从业者交流技术、分享实战经验、获取产品下载密码、享受优惠活动
            </p>
            <div class="flex flex-wrap gap-2 pt-2">
              <span class="inline-flex items-center rounded-full bg-green-500/10 px-3 py-1.5 text-xs font-medium text-green-400 ring-1 ring-inset ring-green-500/20">
                🔒 产品下载密码
              </span>
              <span class="inline-flex items-center rounded-full bg-blue-500/10 px-3 py-1.5 text-xs font-medium text-blue-400 ring-1 ring-inset ring-blue-500/20">
                🎯 技术支持
              </span>
              <span class="inline-flex items-center rounded-full bg-purple-500/10 px-3 py-1.5 text-xs font-medium text-purple-400 ring-1 ring-inset ring-purple-500/20">
                🎉 优惠活动
              </span>
              <span class="inline-flex items-center rounded-full bg-orange-500/10 px-3 py-1.5 text-xs font-medium text-orange-400 ring-1 ring-inset ring-orange-500/20">
                💡 实战经验
              </span>
            </div>
          </div>
          <div class="flex flex-col gap-3">
            <a 
              href="/#contact" 
              class="btn-glow inline-flex h-12 items-center justify-center rounded-xl bg-gradient-to-r from-primary to-purple-600 px-8 text-sm font-semibold text-primary-foreground shadow-lg shadow-primary/25 transition-all hover:shadow-xl hover:shadow-primary/30 hover:scale-105"
            >
              👉 立即加入
            </a>
            <p class="text-xs text-center text-muted-foreground bg-muted/50 px-4 py-1.5 rounded-full">
              群号：897500387
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<!-- 不蒜子统计 -->
<script is:inline async src="//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js"></script>
