# DNS 配置指南 - yyyxxx.cc

## ✅ 已完成的配置

- ✅ Astro 项目已创建
- ✅ GitHub 仓库已创建：https://github.com/YingxueSec/yyyxxx-website
- ✅ 代码已推送到 GitHub
- ✅ GitHub Actions 自动部署已启用
- ✅ GitHub Pages 已配置
- ✅ 自定义域名 yyyxxx.cc 已在 GitHub 设置

## 🔧 下一步：配置 DNS

### 重要提示

由于已经在 GitHub 配置了自定义域名 `yyyxxx.cc`，GitHub Pages 会自动将所有访问重定向到该域名。
**在 DNS 配置生效前，网站将无法访问。**

### DNS 配置步骤

登录你的域名注册商或 DNS 服务提供商（例如：阿里云、腾讯云、Cloudflare、GoDaddy 等）。

#### 添加 A 记录（必需）

添加以下 4 条 A 记录，将 `yyyxxx.cc` 指向 GitHub Pages 的 IP 地址：

| 记录类型 | 主机记录 | 记录值 | TTL |
|---------|---------|--------|-----|
| A | @ | 185.199.108.153 | 600 |
| A | @ | 185.199.109.153 | 600 |
| A | @ | 185.199.110.153 | 600 |
| A | @ | 185.199.111.153 | 600 |

**说明**：
- `@` 表示根域名（yyyxxx.cc）
- TTL 可以设置为 600 秒（10 分钟）或更短，方便快速生效

#### 添加 CNAME 记录（可选，支持 www 子域名）

如果你希望 `www.yyyxxx.cc` 也能访问，添加：

| 记录类型 | 主机记录 | 记录值 | TTL |
|---------|---------|--------|-----|
| CNAME | www | yingxuesec.github.io | 600 |

### 不同 DNS 提供商的配置示例

#### 阿里云（万网）

1. 登录阿里云控制台
2. 进入"域名" > "解析设置"
3. 点击"添加记录"
4. 按上表添加 4 条 A 记录

#### 腾讯云（DNSPod）

1. 登录腾讯云控制台
2. 进入"云解析" > "我的域名"
3. 点击域名进入解析设置
4. 点击"添加记录"
5. 按上表添加 4 条 A 记录

#### Cloudflare

1. 登录 Cloudflare
2. 选择域名 yyyxxx.cc
3. 进入 DNS 设置
4. 点击"Add record"
5. 按上表添加 4 条 A 记录
6. **重要**：确保代理状态为"DNS only"（灰色云朵），不要启用代理

#### GoDaddy

1. 登录 GoDaddy
2. 进入"我的产品" > "域名"
3. 点击域名旁的"DNS"
4. 添加记录，按上表配置

## 🔍 验证 DNS 配置

### 使用命令行验证

```bash
# 查询 A 记录
dig yyyxxx.cc +short

# 或使用 nslookup
nslookup yyyxxx.cc

# 查询 CNAME 记录（如果配置了 www）
dig www.yyyxxx.cc +short
```

**预期结果**：应该返回 4 个 GitHub Pages 的 IP 地址。

### 使用在线工具验证

- https://dnschecker.org/ （全球 DNS 传播检查）
- https://www.whatsmydns.net/ （多地 DNS 查询）

## ⏱️ DNS 生效时间

- **最快**：5-10 分钟（如果 TTL 设置较短）
- **通常**：1-2 小时
- **最长**：24-48 小时（全球完全传播）

## 🔐 启用 HTTPS

DNS 生效后：

1. 访问 https://github.com/YingxueSec/yyyxxx-website/settings/pages
2. 等待 DNS 检查通过（显示绿色勾选）
3. 勾选 **Enforce HTTPS**
4. GitHub 会自动颁发 Let's Encrypt SSL 证书（可能需要几分钟到几小时）

## ✅ 最终验证

DNS 配置生效后，访问以下 URL 验证：

- https://yyyxxx.cc （主域名）
- https://www.yyyxxx.cc （如果配置了 CNAME）

你应该看到一个紫色渐变背景的欢迎页面，显示：
- 🚀 欢迎来到 yyyxxx.cc
- 使用 Astro 构建，部署在 GitHub Pages

## 🎨 自定义网站

编辑以下文件来自定义你的网站：

```bash
# 编辑首页
vim src/pages/index.astro

# 提交并推送
git add .
git commit -m "Update homepage"
git push

# GitHub Actions 会自动重新部署（约 1-2 分钟）
```

## 📊 查看部署状态

```bash
# 查看最近的部署
gh run list --limit 5

# 查看特定部署的详细信息
gh run view

# 在浏览器中打开 Actions 页面
gh repo view --web
```

## 🐛 故障排除

### 问题 1：DNS 配置后仍无法访问

**解决方案**：
1. 清除浏览器缓存
2. 清除 DNS 缓存：
   ```bash
   # macOS
   sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder
   
   # Windows
   ipconfig /flushdns
   
   # Linux
   sudo systemd-resolve --flush-caches
   ```
3. 使用隐私/无痕模式访问
4. 使用手机流量（4G/5G）访问测试

### 问题 2：显示 404 错误

**解决方案**：
1. 检查 GitHub Pages 设置是否正确
2. 确认 `public/CNAME` 文件内容为 `yyyxxx.cc`
3. 重新触发部署：
   ```bash
   git commit --allow-empty -m "Trigger rebuild"
   git push
   ```

### 问题 3：HTTPS 证书错误

**解决方案**：
1. 等待 24 小时让 GitHub 颁发证书
2. 在 GitHub Pages 设置中取消勾选再重新勾选 "Enforce HTTPS"
3. 确保 DNS 记录正确且已生效

### 问题 4：想先测试网站（DNS 配置前）

**临时解决方案**：
1. 临时移除自定义域名配置：
   ```bash
   gh api repos/YingxueSec/yyyxxx-website/pages -X PUT -F cname=
   ```
2. 访问 https://yingxuesec.github.io/yyyxxx-website/
3. 测试完成后重新配置：
   ```bash
   gh api repos/YingxueSec/yyyxxx-website/pages -X PUT -f cname=yyyxxx.cc
   ```

## 📞 获取帮助

- GitHub 仓库：https://github.com/YingxueSec/yyyxxx-website
- GitHub Pages 文档：https://docs.github.com/pages
- Astro 文档：https://docs.astro.build

---

**当前状态**：等待 DNS 配置

配置 DNS 后，你的网站将在 https://yyyxxx.cc 上线！🎉
