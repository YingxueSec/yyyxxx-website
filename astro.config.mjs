import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://yyyxxx.cc',
  // 如果你的仓库名不是 <username>.github.io，需要设置 base
  // base: '/your-repo-name',
});
